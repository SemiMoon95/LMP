import { CheckList } from './checklist';

export class TextboxQuestion extends CheckList<string> {
  controlType = 'textbox';
  type: string;

  constructor(options: {} = {}) {
    super(options);
    this.type = options['type'] || '';
  }
}
